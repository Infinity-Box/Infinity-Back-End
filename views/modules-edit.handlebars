<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <form method="POST" action="/modules/{{module._id}}?_method=PUT">
    <form class="edit-form" action="/modules/{{review.moduleId}}/reviews/{{review._id}}" method="POST">

      {{> modules-form}}
      <!-- BUTTON -->
      <div class="form-group">
        <button class="btn btn-primary" type="submit">Save Module</button>
        <button class="btn btn-primary"><a href="javascript: history.go(-1)">Back</a></button>
      </div>
    </form>
  </div>
</div>